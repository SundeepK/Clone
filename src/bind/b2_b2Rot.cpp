/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Rot
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include <Box2D/Box2D.h>


/** ~b2Rot()
 * 
 */
static int b2Rot__b2Rot(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub::checksdata_d(L, 1, "b2.b2Rot"));
    if (userdata->gc) {
      b2Rot *self = (b2Rot *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "__gc: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "__gc: Unknown exception");
  }
  return dub::error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2Rot__set_(lua_State *L) {

  b2Rot *self = *((b2Rot **)dub::checksdata_n(L, 1, "b2.b2Rot"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 1: {
      if (DUB_ASSERT_KEY(key, "s")) break;
      self->s = luaL_checknumber(L, 3);
      return 0;
    }
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      self->c = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Rot__get_(lua_State *L) {

  b2Rot *self = *((b2Rot **)dub::checksdata_n(L, 1, "b2.b2Rot", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 1: {
      if (DUB_ASSERT_KEY(key, "s")) break;
      lua_pushnumber(L, self->s);
      return 1;
    }
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      lua_pushnumber(L, self->c);
      return 1;
    }
  }
  return 0;
}

/** b2Rot::b2Rot()
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:301
 */
static int b2Rot_b2Rot(lua_State *L) {
  try {
    int top__ = lua_gettop(L);
    if (top__ >= 1) {
      float32 angle = dub::checknumber(L, 1);
      b2Rot *retval__ = new b2Rot(angle);
      dub::pushudata(L, retval__, "b2.b2Rot", true);
      return 1;
    } else {
      b2Rot *retval__ = new b2Rot();
      dub::pushudata(L, retval__, "b2.b2Rot", true);
      return 1;
    }
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Rot::Set(float32 angle)
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:312
 */
static int b2Rot_Set(lua_State *L) {
  try {
    b2Rot *self = *((b2Rot **)dub::checksdata(L, 1, "b2.b2Rot"));
    float32 angle = dub::checknumber(L, 2);
    self->Set(angle);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Set: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Set: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Rot::SetIdentity()
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:320
 */
static int b2Rot_SetIdentity(lua_State *L) {
  try {
    b2Rot *self = *((b2Rot **)dub::checksdata(L, 1, "b2.b2Rot"));
    self->SetIdentity();
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "SetIdentity: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "SetIdentity: Unknown exception");
  }
  return dub::error(L);
}

/** float32 b2Rot::GetAngle() const
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:327
 */
static int b2Rot_GetAngle(lua_State *L) {
  try {
    b2Rot *self = *((b2Rot **)dub::checksdata(L, 1, "b2.b2Rot"));
    lua_pushnumber(L, self->GetAngle());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "GetAngle: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "GetAngle: Unknown exception");
  }
  return dub::error(L);
}

/** b2Vec2 b2Rot::GetXAxis() const
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:333
 */
static int b2Rot_GetXAxis(lua_State *L) {
  try {
    b2Rot *self = *((b2Rot **)dub::checksdata(L, 1, "b2.b2Rot"));
    dub::pushudata(L, new b2Vec2(self->GetXAxis()), "b2.b2Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "GetXAxis: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "GetXAxis: Unknown exception");
  }
  return dub::error(L);
}

/** b2Vec2 b2Rot::GetYAxis() const
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:339
 */
static int b2Rot_GetYAxis(lua_State *L) {
  try {
    b2Rot *self = *((b2Rot **)dub::checksdata(L, 1, "b2.b2Rot"));
    dub::pushudata(L, new b2Vec2(self->GetYAxis()), "b2.b2Vec2", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "GetYAxis: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "GetYAxis: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int b2Rot___tostring(lua_State *L) {
  b2Rot *self = *((b2Rot **)dub::checksdata_n(L, 1, "b2.b2Rot"));
  lua_pushfstring(L, "b2.b2Rot: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Rot_member_methods[] = {
  { "__gc"         , b2Rot__b2Rot         },
  { "__newindex"   , b2Rot__set_          },
  { "__index"      , b2Rot__get_          },
  { "new"          , b2Rot_b2Rot          },
  { "Set"          , b2Rot_Set            },
  { "SetIdentity"  , b2Rot_SetIdentity    },
  { "GetAngle"     , b2Rot_GetAngle       },
  { "GetXAxis"     , b2Rot_GetXAxis       },
  { "GetYAxis"     , b2Rot_GetYAxis       },
  { "__tostring"   , b2Rot___tostring     },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_b2_b2Rot(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.b2Rot");
  // <mt>

  // register member methods
  dub::fregister(L, b2Rot_member_methods);
  // setup meta-table
  dub::setup(L, "b2.b2Rot");
  // <mt>
  return 1;
}
