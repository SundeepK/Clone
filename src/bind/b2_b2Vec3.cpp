/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Vec3
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include <Box2D/Box2D.h>


/** ~b2Vec3()
 * 
 */
static int b2Vec3__b2Vec3(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub::checksdata_d(L, 1, "b2.b2Vec3"));
    if (userdata->gc) {
      b2Vec3 *self = (b2Vec3 *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "__gc: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "__gc: Unknown exception");
  }
  return dub::error(L);
}

/** Set attributes (key, value)
 * 
 */
static int b2Vec3__set_(lua_State *L) {

  b2Vec3 *self = *((b2Vec3 **)dub::checksdata_n(L, 1, "b2.b2Vec3"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "x")) break;
      self->x = luaL_checknumber(L, 3);
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "y")) break;
      self->y = luaL_checknumber(L, 3);
      return 0;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "z")) break;
      self->z = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Vec3__get_(lua_State *L) {

  b2Vec3 *self = *((b2Vec3 **)dub::checksdata_n(L, 1, "b2.b2Vec3", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "x")) break;
      lua_pushnumber(L, self->x);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "y")) break;
      lua_pushnumber(L, self->y);
      return 1;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "z")) break;
      lua_pushnumber(L, self->z);
      return 1;
    }
  }
  return 0;
}

/** b2Vec3::b2Vec3()
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:147
 */
static int b2Vec3_b2Vec3(lua_State *L) {
  try {
    int top__ = lua_gettop(L);
    if (top__ >= 3) {
      float32 x = dub::checknumber(L, 1);
      float32 y = dub::checknumber(L, 2);
      float32 z = dub::checknumber(L, 3);
      b2Vec3 *retval__ = new b2Vec3(x, y, z);
      dub::pushudata(L, retval__, "b2.b2Vec3", true);
      return 1;
    } else {
      b2Vec3 *retval__ = new b2Vec3();
      dub::pushudata(L, retval__, "b2.b2Vec3", true);
      return 1;
    }
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Vec3::SetZero()
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:153
 */
static int b2Vec3_SetZero(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    self->SetZero();
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "SetZero: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "SetZero: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Vec3::Set(float32 x_, float32 y_, float32 z_)
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:156
 */
static int b2Vec3_Set(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    float32 x_ = dub::checknumber(L, 2);
    float32 y_ = dub::checknumber(L, 3);
    float32 z_ = dub::checknumber(L, 4);
    self->Set(x_, y_, z_);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "Set: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "Set: Unknown exception");
  }
  return dub::error(L);
}

/** b2Vec3 b2Vec3::operator-() const
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:159
 */
static int b2Vec3_operator_unm(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    dub::pushudata(L, new b2Vec3(self->operator- ()), "b2.b2Vec3", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "__unm: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "__unm: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Vec3::operator+=(const b2Vec3 &v)
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:162
 */
static int b2Vec3_operator_adde(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    b2Vec3 *v = *((b2Vec3 **)dub::checksdata(L, 2, "b2.b2Vec3"));
    self->operator+=(*v);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "add: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "add: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Vec3::operator-=(const b2Vec3 &v)
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:168
 */
static int b2Vec3_operator_sube(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    b2Vec3 *v = *((b2Vec3 **)dub::checksdata(L, 2, "b2.b2Vec3"));
    self->operator-=(*v);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "sub: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "sub: Unknown exception");
  }
  return dub::error(L);
}

/** void b2Vec3::operator*=(float32 s)
 * /media/sundeep/3 tb Vol/code/Clone-game-repo/Clone-game/vendor/Box2D/Box2D/Common/b2Math.h:174
 */
static int b2Vec3_operator_mule(lua_State *L) {
  try {
    b2Vec3 *self = *((b2Vec3 **)dub::checksdata(L, 1, "b2.b2Vec3"));
    float32 s = dub::checknumber(L, 2);
    self->operator*=(s);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "mul: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "mul: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int b2Vec3___tostring(lua_State *L) {
  b2Vec3 *self = *((b2Vec3 **)dub::checksdata_n(L, 1, "b2.b2Vec3"));
  lua_pushfstring(L, "b2.b2Vec3: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Vec3_member_methods[] = {
  { "__gc"         , b2Vec3__b2Vec3       },
  { "__newindex"   , b2Vec3__set_         },
  { "__index"      , b2Vec3__get_         },
  { "new"          , b2Vec3_b2Vec3        },
  { "SetZero"      , b2Vec3_SetZero       },
  { "Set"          , b2Vec3_Set           },
  { "__unm"        , b2Vec3_operator_unm  },
  { "add"          , b2Vec3_operator_adde },
  { "sub"          , b2Vec3_operator_sube },
  { "mul"          , b2Vec3_operator_mule },
  { "__tostring"   , b2Vec3___tostring    },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_b2_b2Vec3(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.b2Vec3");
  // <mt>

  // register member methods
  dub::fregister(L, b2Vec3_member_methods);
  // setup meta-table
  dub::setup(L, "b2.b2Vec3");
  // <mt>
  return 1;
}
