/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Profile
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include <Box2D/Box2D.h>


/** Set attributes (key, value)
 * 
 */
static int b2Profile__set_(lua_State *L) {

  b2Profile *self = *((b2Profile **)dub::checksdata_n(L, 1, "b2.b2Profile"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub::hash(key, 25);
  switch(key_h) {
    case 10: {
      if (DUB_ASSERT_KEY(key, "step")) break;
      self->step = luaL_checknumber(L, 3);
      return 0;
    }
    case 13: {
      if (DUB_ASSERT_KEY(key, "collide")) break;
      self->collide = luaL_checknumber(L, 3);
      return 0;
    }
    case 22: {
      if (DUB_ASSERT_KEY(key, "solve")) break;
      self->solve = luaL_checknumber(L, 3);
      return 0;
    }
    case 11: {
      if (DUB_ASSERT_KEY(key, "solveInit")) break;
      self->solveInit = luaL_checknumber(L, 3);
      return 0;
    }
    case 15: {
      if (DUB_ASSERT_KEY(key, "solveVelocity")) break;
      self->solveVelocity = luaL_checknumber(L, 3);
      return 0;
    }
    case 16: {
      if (DUB_ASSERT_KEY(key, "solvePosition")) break;
      self->solvePosition = luaL_checknumber(L, 3);
      return 0;
    }
    case 21: {
      if (DUB_ASSERT_KEY(key, "broadphase")) break;
      self->broadphase = luaL_checknumber(L, 3);
      return 0;
    }
    case 6: {
      if (DUB_ASSERT_KEY(key, "solveTOI")) break;
      self->solveTOI = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Profile__get_(lua_State *L) {

  b2Profile *self = *((b2Profile **)dub::checksdata_n(L, 1, "b2.b2Profile", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub::hash(key, 25);
  switch(key_h) {
    case 10: {
      if (DUB_ASSERT_KEY(key, "step")) break;
      lua_pushnumber(L, self->step);
      return 1;
    }
    case 13: {
      if (DUB_ASSERT_KEY(key, "collide")) break;
      lua_pushnumber(L, self->collide);
      return 1;
    }
    case 22: {
      if (DUB_ASSERT_KEY(key, "solve")) break;
      lua_pushnumber(L, self->solve);
      return 1;
    }
    case 11: {
      if (DUB_ASSERT_KEY(key, "solveInit")) break;
      lua_pushnumber(L, self->solveInit);
      return 1;
    }
    case 15: {
      if (DUB_ASSERT_KEY(key, "solveVelocity")) break;
      lua_pushnumber(L, self->solveVelocity);
      return 1;
    }
    case 16: {
      if (DUB_ASSERT_KEY(key, "solvePosition")) break;
      lua_pushnumber(L, self->solvePosition);
      return 1;
    }
    case 21: {
      if (DUB_ASSERT_KEY(key, "broadphase")) break;
      lua_pushnumber(L, self->broadphase);
      return 1;
    }
    case 6: {
      if (DUB_ASSERT_KEY(key, "solveTOI")) break;
      lua_pushnumber(L, self->solveTOI);
      return 1;
    }
  }
  return 0;
}

/** b2Profile()
 * 
 */
static int b2Profile_b2Profile(lua_State *L) {
  try {
    b2Profile *retval__ = new b2Profile();
    dub::pushudata(L, retval__, "b2.b2Profile", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int b2Profile___tostring(lua_State *L) {
  b2Profile *self = *((b2Profile **)dub::checksdata_n(L, 1, "b2.b2Profile"));
  lua_pushfstring(L, "b2.b2Profile: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Profile_member_methods[] = {
  { "__newindex"   , b2Profile__set_      },
  { "__index"      , b2Profile__get_      },
  { "new"          , b2Profile_b2Profile  },
  { "__tostring"   , b2Profile___tostring },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_b2_b2Profile(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.b2Profile");
  // <mt>

  // register member methods
  dub::fregister(L, b2Profile_member_methods);
  // setup meta-table
  dub::setup(L, "b2.b2Profile");
  // <mt>
  return 1;
}
