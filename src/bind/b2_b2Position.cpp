/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2Position
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include <Box2D/Box2D.h>


/** Set attributes (key, value)
 * 
 */
static int b2Position__set_(lua_State *L) {

  b2Position *self = *((b2Position **)dub::checksdata_n(L, 1, "b2.b2Position"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      self->c = **((b2Vec2 **)dub::checksdata_n(L, 3, "b2.b2Vec2"));
      return 0;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "a")) break;
      self->a = luaL_checknumber(L, 3);
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2Position__get_(lua_State *L) {

  b2Position *self = *((b2Position **)dub::checksdata_n(L, 1, "b2.b2Position", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub::hash(key, 3);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "c")) break;
      dub::pushudata(L, &self->c, "b2.b2Vec2", false);
      return 1;
    }
    case 1: {
      if (DUB_ASSERT_KEY(key, "a")) break;
      lua_pushnumber(L, self->a);
      return 1;
    }
  }
  return 0;
}

/** b2Position()
 * 
 */
static int b2Position_b2Position(lua_State *L) {
  try {
    b2Position *retval__ = new b2Position();
    dub::pushudata(L, retval__, "b2.b2Position", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int b2Position___tostring(lua_State *L) {
  b2Position *self = *((b2Position **)dub::checksdata_n(L, 1, "b2.b2Position"));
  lua_pushfstring(L, "b2.b2Position: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2Position_member_methods[] = {
  { "__newindex"   , b2Position__set_     },
  { "__index"      , b2Position__get_     },
  { "new"          , b2Position_b2Position },
  { "__tostring"   , b2Position___tostring },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_b2_b2Position(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.b2Position");
  // <mt>

  // register member methods
  dub::fregister(L, b2Position_member_methods);
  // setup meta-table
  dub::setup(L, "b2.b2Position");
  // <mt>
  return 1;
}
