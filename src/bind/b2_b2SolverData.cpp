/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class b2SolverData
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include <Box2D/Box2D.h>


/** Set attributes (key, value)
 * 
 */
static int b2SolverData__set_(lua_State *L) {

  b2SolverData *self = *((b2SolverData **)dub::checksdata_n(L, 1, "b2.b2SolverData"));
  const char *key = luaL_checkstring(L, 2);
  int key_h = dub::hash(key, 4);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "step")) break;
      self->step = **((b2TimeStep **)dub::checksdata_n(L, 3, "b2.b2TimeStep"));
      return 0;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "positions")) break;
      dub::protect(L, 1, 3, "positions");
      self->positions = *((b2Position **)dub::checksdata_n(L, 3, "b2.b2Position"));
      return 0;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "velocities")) break;
      dub::protect(L, 1, 3, "velocities");
      self->velocities = *((b2Velocity **)dub::checksdata_n(L, 3, "b2.b2Velocity"));
      return 0;
    }
  }
  if (lua_istable(L, 1)) {
    lua_rawset(L, 1);
  } else {
    luaL_error(L, KEY_EXCEPTION_MSG, key);
  }
  return 0;
}

/** Get attributes (key)
 * 
 */
static int b2SolverData__get_(lua_State *L) {

  b2SolverData *self = *((b2SolverData **)dub::checksdata_n(L, 1, "b2.b2SolverData", true));
  const char *key = luaL_checkstring(L, 2);
  // <self> "key" <mt>
  // rawget(mt, key)
  lua_pushvalue(L, 2);
  // <self> "key" <mt> "key"
  lua_rawget(L, -2);
  if (!lua_isnil(L, -1)) {
    // Found method.
    return 1;
  } else {
    // Not in mt = attribute access.
    lua_pop(L, 2);
  }
  int key_h = dub::hash(key, 4);
  switch(key_h) {
    case 0: {
      if (DUB_ASSERT_KEY(key, "step")) break;
      dub::pushudata(L, &self->step, "b2.b2TimeStep", false);
      return 1;
    }
    case 2: {
      if (DUB_ASSERT_KEY(key, "positions")) break;
      b2Position *retval__ = self->positions;
      if (!retval__) return 0;
      dub::pushudata(L, retval__, "b2.b2Position", false);
      return 1;
    }
    case 3: {
      if (DUB_ASSERT_KEY(key, "velocities")) break;
      b2Velocity *retval__ = self->velocities;
      if (!retval__) return 0;
      dub::pushudata(L, retval__, "b2.b2Velocity", false);
      return 1;
    }
  }
  return 0;
}

/** b2SolverData()
 * 
 */
static int b2SolverData_b2SolverData(lua_State *L) {
  try {
    b2SolverData *retval__ = new b2SolverData();
    dub::pushudata(L, retval__, "b2.b2SolverData", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int b2SolverData___tostring(lua_State *L) {
  b2SolverData *self = *((b2SolverData **)dub::checksdata_n(L, 1, "b2.b2SolverData"));
  lua_pushfstring(L, "b2.b2SolverData: %p", self);
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg b2SolverData_member_methods[] = {
  { "__newindex"   , b2SolverData__set_   },
  { "__index"      , b2SolverData__get_   },
  { "new"          , b2SolverData_b2SolverData },
  { "__tostring"   , b2SolverData___tostring },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_b2_b2SolverData(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "b2.b2SolverData");
  // <mt>

  // register member methods
  dub::fregister(L, b2SolverData_member_methods);
  // setup meta-table
  dub::setup(L, "b2.b2SolverData");
  // <mt>
  return 1;
}
